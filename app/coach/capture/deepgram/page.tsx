'use client';

import DeepgramTranscriptionDisplay from '../components/DeepgramTranscriptionDisplay';

export default function DeepgramTranscriptionPage() {
  return (
    <div className="min-h-screen" style={{ backgroundColor: 'var(--night)' }}>
      <DeepgramTranscriptionDisplay />
    </div>
  );
} 